const horas = 2;
const minutos = 60;
let seg = 0;

console.info('Horas: ', horas);
console.info('Minutos: ', minutos);
seg = horas * 3600 + minutos * 60;
console.info('Conversion a segundos: ', seg, 'seg');
